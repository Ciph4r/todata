
// A function that adds an item to our todo list.
const addTodo = function(todo) {
  // todos.push(todo);
  currentTodos[0].push(todo)
}




const newaddtodos = (num , todo) =>{
  currentTodos[num].push(todo)
}